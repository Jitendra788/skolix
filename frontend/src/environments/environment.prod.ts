export const environment = {
  production: true,
  /**
   * FastAPI origin (no trailing slash), e.g. https://skolix-api.onrender.com
   * Set via Vercel env `API_ORIGIN` — prebuild writes this value.
   */
  apiOrigin: 'https://REPLACE_WITH_YOUR_API_ORIGIN',
};
