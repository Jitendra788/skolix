/** Section letters for roster and fee register filters. */
export const SECTION_LETTERS = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H'] as const;

/** Default section presets when configuring a class (A–F). */
export const DEFAULT_CLASS_SECTION_PRESETS = ['A', 'B', 'C', 'D', 'E', 'F'] as const;
